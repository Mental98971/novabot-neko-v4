# ── Telegram ─────────────────────────────────────────────────────
python-telegram-bot[job-queue]>=21.0
pyrogram>=2.0.106
tgcrypto>=1.2.5
py-tgcalls>=1.2.0

# ── Database ─────────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.20.0
asyncpg>=0.29.0
alembic>=1.13.0
redis>=5.0.0

# ── AI Providers ─────────────────────────────────────────────────
openai>=1.12.0
anthropic>=0.18.0
google-generativeai>=0.4.0

# ── Media ────────────────────────────────────────────────────────
yt-dlp>=2024.7.0
pillow>=10.4.0
aiohttp>=3.9.0
aiofiles>=23.2.0
aiohttp-socks>=0.8.0

# ── Utilities ────────────────────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.8.0
pydantic-settings>=2.7.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.1.0
orjson>=3.10.0
cachetools>=5.3.0
uvloop>=0.19.0; sys_platform != "win32"

# ── Monitoring ───────────────────────────────────────────────────
structlog>=24.4.0
prometheus-client>=0.20.0

# NOTE: sentry-sdk, tenacity, cryptography, argon2-cffi, spotipy,
# lyricsgenius, mutagen, and motor appeared in the original uploads'
# requirements files but weren't actually imported by any code that
# shipped with them (error tracking, Spotify/lyrics resolution, and a
# Mongo-backed queue store were declared but not implemented, or were
# superseded — see README "Not carried over"). Add them back if you
# build out those features.
